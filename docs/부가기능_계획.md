# 부가 기능 계획 (Phase 2)

> **프로젝트**: AI Audio Mixing & Conversion System  
> **작성일**: 2026-01-29  
> **버전**: 1.0  
> **상태**: Phase 1 완료 후 구현 예정

---

## 📋 목차

1. [개요](#1-개요)
2. [부가 기능 목록](#2-부가-기능-목록)
3. [상세 구현 계획](#3-상세-구현-계획)
4. [기술 스택](#4-기술-스택)
5. [예상 일정](#5-예상-일정)
6. [참고 자료](#6-참고-자료)

---

## 1. 개요

### 1.1 목적

Phase 1에서 완성된 핵심 기능(Stem Separation, 오디오 분석, 트랜지션 시스템)을 기반으로, 사용자 경험을 향상시키고 고급 기능을 추가하는 Phase 2 개발 계획입니다.

### 1.2 개발 원칙

- **Phase 1 완료 후 시작**: 핵심 기능이 안정적으로 동작한 후에만 부가 기능 개발 시작
- **사용자 피드백 기반**: Phase 1 사용자 테스트 결과를 반영하여 우선순위 결정
- **점진적 확장**: 한 번에 모든 기능을 구현하지 않고, 중요도 순으로 단계적 추가

---

## 2. 부가 기능 목록

### 2.1 MIDI 변환 기능

#### 2.1.1 다성 MIDI 변환 (Basic Pitch)

- **설명**: 분리된 Stem을 MIDI 데이터로 변환
- **우선순위**: 높음
- **예상 작업량**: 2-3일

**주요 기능:**

- Bass Stem → MIDI 변환
- Vocals Stem → MIDI 변환
- Other Stem → MIDI 변환
- 비트 그리드 기반 퀀타이즈 (Quantization)
- MIDI 파일 내보내기

**기술적 특징:**

- Spotify의 Basic Pitch 모델 활용
- CQT 기반 다성(Polyphonic) 인식
- 피치 벤드(Pitch Bend) 캡처 지원
- 악기 무관(Instrument Agnostic) 처리

#### 2.1.2 MIDI 편집 기능

- **설명**: 변환된 MIDI 데이터 편집
- **우선순위**: 중간
- **예상 작업량**: 3-4일

**주요 기능:**

- MIDI 노트 편집 (추가/삭제/수정)
- 벨로시티 조정
- 음표 길이 조정
- 옥타브 변경
- MIDI 채널 분리

### 2.2 사용자 인터페이스 고도화

#### 2.2.1 인터랙티브 파형 시각화

- **설명**: 분석 결과를 파형 위에 오버레이하여 표시
- **우선순위**: 높음
- **예상 작업량**: 2-3일

**주요 기능:**

- 비트 그리드 시각화 (파형 위에 비트 마커 표시)
- 다운비트 하이라이트
- 구조 분할 구간 표시 (Intro, Verse, Chorus, Outro)
- 믹싱 포인트(Swap Point) 드래그 앤 드롭 수정
- 실시간 파형 스크롤 및 줌

**기술 스택:**

- Web Audio API
- Canvas API 또는 WebGL
- React 컴포넌트

#### 2.2.2 실시간 미리듣기

- **설명**: 렌더링 전 트랜지션 구간 미리듣기
- **우선순위**: 중간
- **예상 작업량**: 2일

**주요 기능:**

- 저음질/저지연 모드 프리뷰
- 트랜지션 구간만 선택하여 재생
- BPM 변경 실시간 반영
- Swap Point 변경 시 즉시 업데이트

**기술 스택:**

- FFmpeg 저해상도 렌더링
- Web Audio API 스트리밍
- WebSocket 또는 Server-Sent Events

### 2.3 고급 믹싱 기능

#### 2.3.1 다중 트랙 믹싱

- **설명**: 2개 이상의 트랙을 동시에 믹싱
- **우선순위**: 낮음
- **예상 작업량**: 4-5일

**주요 기능:**

- 3개 이상 트랙 선택
- 순차적 트랜지션 체인 생성
- 각 트랜지션 포인트 개별 설정
- 전체 믹스 길이 자동 계산

#### 2.3.2 커스텀 이펙트 적용

- **설명**: 트랜지션에 이펙트 추가
- **우선순위**: 낮음
- **예상 작업량**: 3-4일

**주요 기능:**

- 리버브(Reverb) 적용
- 딜레이(Delay) 적용
- 필터(Filter) 스위핑
- 이펙트 파라미터 조정

### 2.4 성능 최적화

#### 2.4.1 비동기 작업 큐 시스템

- **설명**: Redis + Celery 기반 작업 큐
- **우선순위**: 높음 (대용량 파일 처리 시 필수)
- **예상 작업량**: 3-4일

**주요 기능:**

- 작업 상태 추적 (pending, processing, completed, failed)
- 진행률 표시 (Progress Bar)
- 작업 취소 기능
- 에러 재시도 로직
- GPU/CPU 워커 분리

**기술 스택:**

- Redis (메시지 브로커)
- Celery (작업 큐)
- FastAPI 또는 Express.js (API Gateway)

#### 2.4.2 배치 처리

- **설명**: 여러 트랙을 한 번에 처리
- **우선순위**: 중간
- **예상 작업량**: 2일

**주요 기능:**

- 여러 트랙 동시 업로드
- 배치 Stem 분리
- 배치 분석
- 진행 상황 모니터링

### 2.5 데이터 관리 기능

#### 2.5.1 프로젝트 저장/로드

- **설명**: 믹싱 프로젝트 저장 및 재편집
- **우선순위**: 중간
- **예상 작업량**: 2-3일

**주요 기능:**

- 프로젝트 메타데이터 저장
- 트랙 선택 및 Swap Point 저장
- 프로젝트 목록 조회
- 프로젝트 삭제/수정
- 프로젝트 공유

#### 2.5.2 히스토리 관리

- **설명**: 처리 이력 관리
- **우선순위**: 낮음
- **예상 작업량**: 1-2일

**주요 기능:**

- 업로드한 트랙 목록
- 생성한 트랜지션 목록
- 최근 작업 목록
- 즐겨찾기 기능

### 2.6 고급 분석 기능

#### 2.6.1 에너지 레벨 분석

- **설명**: 곡의 에너지 변화 분석
- **우선순위**: 낮음
- **예상 작업량**: 2일

**주요 기능:**

- 에너지 곡선 시각화
- 드롭(Drop) 구간 자동 감지
- 빌드업(Build-up) 구간 감지
- 최적 믹싱 포인트 추천

#### 2.6.2 하모닉 분석 고도화

- **설명**: 더 정확한 조성 분석
- **우선순위**: 낮음
- **예상 작업량**: 2-3일

**주요 기능:**

- 코드 진행(Chord Progression) 분석
- 조바꿈(Key Change) 감지
- 하모닉 믹싱 추천 개선

---

## 3. 상세 구현 계획

### 3.1 MIDI 변환 모듈

#### 3.1.1 Basic Pitch 통합

**파일 구조:**

```
server/
├── services/
│   ├── midi_conversion.py      # Basic Pitch 통합
│   └── midi_quantization.py    # 비트 그리드 기반 퀀타이즈
├── models/
│   └── MidiFile.js             # MIDI 파일 모델
└── routes/
    └── midi.js                 # MIDI 변환 API
```

**API 엔드포인트:**

- `POST /api/midi/convert`: Stem을 MIDI로 변환
- `GET /api/midi/:id`: 변환된 MIDI 파일 다운로드
- `POST /api/midi/quantize`: MIDI 퀀타이즈

**구현 예시:**

```python
# server/services/midi_conversion.py
from basic_pitch.inference import predict_and_save
import librosa

def convert_stem_to_midi(stem_path, output_dir, bpm_grid=None):
    """
    Stem을 MIDI로 변환하고 비트 그리드에 맞춰 퀀타이즈

    Args:
        stem_path: Stem 파일 경로
        output_dir: MIDI 출력 디렉토리
        bpm_grid: 비트 그리드 정보 (퀀타이즈용)
    """
    # Basic Pitch로 MIDI 변환
    predict_and_save(
        audio_path_list=[stem_path],
        output_directory=output_dir,
        save_midi=True,
        onset_threshold=0.6,
        frame_threshold=0.4
    )

    # 비트 그리드 기반 퀀타이즈 (선택적)
    if bpm_grid:
        quantize_midi_to_grid(output_dir, bpm_grid)

    return output_dir
```

#### 3.1.2 데이터베이스 스키마 추가

```sql
-- MIDI 파일 저장
CREATE TABLE MidiFiles (
    id INTEGER PRIMARY KEY AUTO_INCREMENT,
    track_id INTEGER NOT NULL,
    stem_type VARCHAR(20) NOT NULL,  -- 'drums', 'bass', 'vocals', 'other'
    file_path VARCHAR(500) NOT NULL,
    file_size INTEGER,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (track_id) REFERENCES Tracks(id) ON DELETE CASCADE
);
```

### 3.2 인터랙티브 파형 시각화

#### 3.2.1 컴포넌트 구조

```
client/src/components/
├── WaveformVisualizer/
│   ├── WaveformVisualizer.jsx      # 메인 컴포넌트
│   ├── BeatGridOverlay.jsx         # 비트 그리드 오버레이
│   ├── StructureMarker.jsx         # 구조 분할 마커
│   ├── SwapPointControl.jsx        # Swap Point 드래그 컨트롤
│   └── WaveformVisualizer.module.css
```

**주요 기능 구현:**

```javascript
// client/src/components/WaveformVisualizer/WaveformVisualizer.jsx
import { useEffect, useRef } from "react";
import { useAudioAnalysis } from "../../hooks/useAudioAnalysis";

const WaveformVisualizer = ({ audioUrl, analysis, onSwapPointChange }) => {
  const canvasRef = useRef(null);
  const { beats, downbeats, segments } = analysis;

  useEffect(() => {
    // 파형 렌더링
    renderWaveform(audioUrl, canvasRef.current);

    // 비트 그리드 오버레이
    renderBeatGrid(beats, downbeats, canvasRef.current);

    // 구조 분할 마커
    renderStructureMarkers(segments, canvasRef.current);
  }, [audioUrl, analysis]);

  return (
    <div className={styles.waveformContainer}>
      <canvas ref={canvasRef} />
      <SwapPointControl
        swapPoint={analysis.swapPoint}
        onChange={onSwapPointChange}
      />
    </div>
  );
};
```

### 3.3 비동기 작업 큐 시스템

#### 3.3.1 아키텍처

```
┌─────────────┐
│  API Server │ (Express.js)
└──────┬──────┘
       │ 작업 요청
       ▼
┌─────────────┐
│    Redis    │ (메시지 브로커)
└──────┬──────┘
       │ 작업 큐
       ├─────────────┬─────────────┐
       ▼             ▼             ▼
  ┌────────┐    ┌────────┐    ┌────────┐
  │ GPU    │    │ CPU    │    │ CPU    │
  │ Worker │    │ Worker │    │ Worker │
  └────────┘    └────────┘    └────────┘
```

#### 3.3.2 구현 예시

```python
# server/workers/stem_worker.py
from celery import Celery
from server.services.stem_separation import split_track_layers

celery_app = Celery('audio_processing', broker='redis://localhost:6379/0')

@celery_app.task(queue='gpu_queue')
def process_stem_separation(track_id, audio_path, output_dir):
    """
    GPU 워커에서 실행되는 Stem 분리 작업
    """
    try:
        layers = split_track_layers(audio_path, output_dir, device='cuda')

        # 데이터베이스에 결과 저장
        save_stems_to_db(track_id, stems)

        return {
            'status': 'completed',
            'track_id': track_id,
            'layers': layers
        }
    except Exception as e:
        return {
            'status': 'failed',
            'error': str(e)
        }
```

```javascript
// server/routes/audio.js
const { process_stem_separation } = require("../workers/stem_worker");

router.post("/api/sound/split", async (req, res) => {
  const { trackId } = req.body;

  // 비동기 작업 큐에 추가
  const task = process_layer_separation.delay(trackId, audioPath, outputDir);

  res.json({
    success: true,
    taskId: task.id,
    status: "pending",
    message: "레이어 분리 작업이 큐에 추가되었습니다.",
  });
});

router.get("/api/sound/split/:taskId/status", async (req, res) => {
  const { taskId } = req.params;
  const task = process_stem_separation.AsyncResult(taskId);

  res.json({
    status: task.state, // PENDING, STARTED, SUCCESS, FAILURE
    progress: task.info?.progress || 0,
    result: task.result,
  });
});
```

---

## 4. 기술 스택

### 4.1 MIDI 변환

- **Basic Pitch**: Spotify의 오픈소스 MIDI 변환 모델
- **mido**: MIDI 파일 처리 라이브러리
- **music21**: 음악 이론 및 분석 라이브러리 (퀀타이즈용)

### 4.2 시각화

- **Web Audio API**: 오디오 데이터 추출
- **Canvas API**: 파형 렌더링
- **React**: UI 컴포넌트
- **D3.js** (선택적): 고급 시각화

### 4.3 작업 큐

- **Redis**: 메시지 브로커
- **Celery**: Python 작업 큐
- **Bull** (선택적): Node.js 작업 큐 (Express.js 사용 시)

### 4.4 스토리지

- **MinIO** (로컬) 또는 **AWS S3**: 대용량 파일 저장
- **PostgreSQL**: 메타데이터 및 작업 상태 저장

---

## 5. 예상 일정

### Phase 2.1 (2주)

- [ ] MIDI 변환 기능 (Basic Pitch 통합)
- [ ] 인터랙티브 파형 시각화
- [ ] 실시간 미리듣기

### Phase 2.2 (2주)

- [ ] 비동기 작업 큐 시스템 (Redis + Celery)
- [ ] 배치 처리 기능
- [ ] 프로젝트 저장/로드

### Phase 2.3 (2주)

- [ ] 다중 트랙 믹싱
- [ ] 커스텀 이펙트 적용
- [ ] 에너지 레벨 분석

**총 예상 기간**: 6주 (Phase 1 완료 후)

---

## 6. 참고 자료

### 6.1 Basic Pitch

- GitHub: https://github.com/spotify/basic-pitch
- 문서: https://github.com/spotify/basic-pitch/blob/main/README.md
- Hugging Face: https://huggingface.co/spotify/basic-pitch

### 6.2 Celery & Redis

- Celery 문서: https://docs.celeryproject.org/
- Redis 문서: https://redis.io/docs/
- Docker + Celery 가이드: https://testdriven.io/courses/fastapi-celery/

### 6.3 Web Audio API

- MDN 문서: https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API
- Web Audio API 가이드: https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API/Using_Web_Audio_API

### 6.4 Canvas API

- MDN 문서: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API
- Canvas 튜토리얼: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API/Tutorial

---

## 7. 우선순위 매트릭스

| 기능               | 우선순위 | 예상 작업량 | 비즈니스 가치 | 기술 난이도 |
| ------------------ | -------- | ----------- | ------------- | ----------- |
| MIDI 변환          | 높음     | 2-3일       | 높음          | 중간        |
| 인터랙티브 파형    | 높음     | 2-3일       | 높음          | 중간        |
| 작업 큐 시스템     | 높음     | 3-4일       | 높음          | 높음        |
| 실시간 미리듣기    | 중간     | 2일         | 중간          | 중간        |
| 프로젝트 저장/로드 | 중간     | 2-3일       | 중간          | 낮음        |
| 배치 처리          | 중간     | 2일         | 중간          | 중간        |
| 다중 트랙 믹싱     | 낮음     | 4-5일       | 낮음          | 높음        |
| 커스텀 이펙트      | 낮음     | 3-4일       | 낮음          | 중간        |
| 에너지 분석        | 낮음     | 2일         | 낮음          | 낮음        |

---

## 8. 의존성 추가

### Python (requirements.txt)

```txt
# Phase 2 추가 의존성
basic-pitch>=1.0.0
mido>=1.2.10
music21>=9.1.0
celery>=5.3.0
redis>=5.0.0
```

### Node.js (package.json)

```json
{
  "dependencies": {
    "bull": "^4.11.0",
    "ioredis": "^5.3.0"
  }
}
```

---

**문서 버전**: 1.0  
**최종 수정일**: 2026-01-29  
**작성자**: Development Team
